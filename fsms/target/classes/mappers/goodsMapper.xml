<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goodsMapper">
		
	<select id="selectViewCnt" parameterType="hashmap" resultType="int">
		<choose>
					<when test='searchWord != null &amp;&amp; searchWord.equals("")'>
						SELECT
								COUNT(*)
						FROM
								GOODS
					</when>
					<otherwise>
						SELECT
								COUNT(*)
						FROM
								GOODS
						WHERE
								GOODS_NAME 		LIKE CONCAT ('%' , #{word} , '%') OR
								GOODS_INFO 	 	LIKE CONCAT ('%' , #{word} , '%') OR
								GOODS_ORIGIN	LIKE CONCAT ('%' , #{word} , '%') OR
								GOODS_CATEGORY  LIKE CONCAT ('%' , #{word} , '%') 
					</otherwise>
		</choose>
	</select>
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
	<select id="selectListGoods" parameterType="GoodsDto" resultType="GoodsDto">
		SELECT
				*
		FROM
				GOODS
		<where>
			<if test='!goodsCategory.equals("all")'>
				GOODS_CATEGORY = #{goodsCategory}
			</if>
			<if test='goodsCategory.equals("all")'>
				GOODS_CATEGORY IN ('beef' , 'lamb' , 'pork' , 'chicken' , 'duckMeat');
			</if>
		</where>
	</select>
	
	
	<select id="selectGetOneGoods" parameterType="int" resultType="GoodsDto">
		SELECT
				*
		FROM
				GOODS
		WHERE
				GOODS_CD = #{goodsCd}
	</select>
	
	<select id="selectGetGoodsCnt" resultType="int" parameterType="int">
		SELECT 
				COUNT(*) 
		FROM 
				GOODS
	</select>
	
	
	<select id="selectGetSearchGoods" resultType="GoodsDto" parameterType="hashmap">

		<if test='method == "price"'>
			SELECT 
		   			*
		    FROM 
					GOODS
			WHERE
					PRICE BETWEEN #{min} AND #{max} 
			ORDER BY
					PRICE
		</if>
		<if test='method == "keyword"'>
			SELECT 
		   			*
		    FROM 
					GOODS
			ORDER BY
				<if test='keyword == "highPrice"'>
					PRICE DESC
				</if>
				<if test='keyword == "lowPrice"'>
					PRICE ASC
				</if>
		</if>
		<if test='method == "search"'>
			SELECT
					*
			FROM
					GOODS
			WHERE
					GOODS_NAME 		LIKE CONCAT ('%' , #{word} , '%') OR
					GOODS_INFO 	 	LIKE CONCAT ('%' , #{word} , '%') OR
					GOODS_ORIGIN	LIKE CONCAT ('%' , #{word} , '%') OR
					GOODS_CATEGORY  LIKE CONCAT ('%' , #{word} , '%') 
		</if>
				
	</select>
	
	
</mapper>